name = "rent-agent-telegram"
main = "src/index.ts"
compatibility_date = "2024-09-01"

# TELEGRAM_BOT_TOKEN, TELEGRAM_CHAT_ID, GITHUB_TOKEN, GITHUB_REPO
# are stored as secrets via: npx wrangler secret put <NAME>
