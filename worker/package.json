{
  "name": "rent-agent-telegram-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "echo 'Wrangler handles TypeScript compilation'",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "setup-webhook": "node setup-webhook.js"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240919.0",
    "wrangler": "^3.78.0"
  }
}
